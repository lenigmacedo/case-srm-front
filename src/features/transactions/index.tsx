import { Card } from "@/components/ui";

export function TabTransacoes() {
  return (
    <Card title="Histórico de transações">
      <p style={{ color: "var(--text-muted)", fontSize: 13 }}>
        Em breve — tabela de transações com filtros e paginação server-side.
      </p>
    </Card>
  );
}
