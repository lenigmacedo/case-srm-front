import { Card } from "@/components/ui";

export function TabSimulador() {
  return (
    <Card title="Simulador de precificação">
      <p style={{ color: "var(--text-muted)", fontSize: 13 }}>
        Em breve — formulário de simulação com cálculo de VP em tempo real.
      </p>
    </Card>
  );
}
